import React from 'react';
import styled from 'styled-components';

function Meetings(): JSX.Element {
  return <Container>Meetings</Container>;
}

export default Meetings;

// Styles 👕🧦👗🧣🧥👔 -->

const Container = styled.div``;

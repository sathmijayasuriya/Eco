export const initializeSentry = async (): Promise<void> => {
  // if (Sentry && process.env.ENV === 'prod') {
  //   Sentry.init({
  //     dsn: sentryDSN,
  //     environment: isProduction ? 'production' : 'staging',
  //   });
  // }
};
